#include<stdio.h>

int main(){

    float l , b, r;
    printf("What is the length: ");
    scanf("%f", &l);
    printf("What is the width: ");
    scanf("%f", &b);
    r = b*l;

    printf("The the area of the rectangle is: %f", r);


}